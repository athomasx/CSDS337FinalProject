extern putchard(char);
extern printd(double);
def testOne(n)
	for i = 1, i < n, 1.0 in
		putchard(42);
	for i = 1, i < n, 1.0 in
		printd(i);
testOne(100);


def testTwo(n)
	x = 0;
	y = 0;
	for i = 1, i < n, 1.0 in
		x = x * 2;
	for i = 1, i < n, 2.0 in
		y = y * 2;
testTwo(20);
		
